// The end-of-turn ground battle resolver now lives in scr_battle_place (so it rides on a
// single, IDE-tracked script asset). This file is intentionally empty and not registered
// in the project. See scr_battle_place for resolve_all_ground_battles() and friends.
